# http://tldp.org/LDP/abs/html/sample-bashrc.html

# Easily ask for confirmation
#	if ask "Are you sure you want to do this?"
#		then echo "You did the thing"
#	fi
function ask() {
    echo -n "$@" '[y/n] ' ; read ans
    case "$ans" in
        y*|Y*) return 0 ;;
        *) return 1 ;;
    esac
}
